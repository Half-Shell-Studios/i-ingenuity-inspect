# Axios base URL. The iOS simulator can use localhost.
# On a physical device, use your Mac's LAN IP instead, e.g. http://192.168.x.x:8130/api/v1
EXPO_PUBLIC_API_URL=http://localhost:8130/api/v1

# Optional. Prefills the login screen in development.
EXPO_PUBLIC_DEV_USER=
EXPO_PUBLIC_DEV_USER_PASS=
